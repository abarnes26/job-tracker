All Jobs for All Companies

<% @jobs.each do |job| %>
<ul><%= link_to "#{job.title}", company_job_path(job.company, job) %> for <%= link_to job.company.name, company_path(job.company)%> in <%= job.city %></ul>
<% end %>
